export const DateUtils={getTodayDate:()=>new Date,getTodayDateString(){const today=this.getTodayDate();return`${today.getFullYear()}-${String(today.getMonth()+1).padStart(2,"0")}-${String(today.getDate()).padStart(2,"0")}`},getTomorrowDate(){const today=this.getTodayDate(),nextDay=new Date(today);return nextDay.setDate(today.getDate()+1),nextDay},getTomorrowDateString(){const nextDay=this.getTomorrowDate();return`${nextDay.getFullYear()}-${String(nextDay.getMonth()+1).padStart(2,"0")}-${String(nextDay.getDate()).padStart(2,"0")}`},setMinDate(element){element?element.setAttribute("min",this.getTomorrowDateString()):console.warn("Элемент не найден")},setMaxDate(element){element?element.setAttribute("max",this.getTodayDateString()):console.warn("Элемент не найден")}};export function calculateDateDifference(date){if(!date)return null;let now=new Date,difference=new Date(date)-now;return Math.ceil(difference/864e5)}export function formatDateCard(date){if(!date)return null;date=new Date(date);return`${String(date.getDate()).padStart(2,"0")}.${String(date.getMonth()+1).padStart(2,"0")}.${date.getFullYear()}`}export function validateCalculator(dateStart,days,months){let isValid=!0;return dateStart.value?clearError(dateStart):(showError(dateStart,"Выберите дату изготовления"),isValid=!1),days.value||months.value?(clearError(days),clearError(months)):(showError(days,"Заполните хотя бы одно поле: дни или месяцы"),showError(months,""),isValid=!1),isValid}export function showError(input,message){if(clearError(input),input.style.borderColor="#e74c3c",input.style.boxShadow="0 0 0 3px rgba(231, 76, 60, 0.1)",message){const error=document.createElement("div");error.className="error-message",error.textContent=message,input.insertAdjacentElement("afterend",error)}}export function clearError(input){input.style.borderColor="#e0e0e0",input.style.boxShadow="";const error=input.nextElementSibling;error&&error.classList.contains("error-message")&&error.remove()}export function calculateExpirationDate(startDate,days,months){let endDate=new Date(startDate);return months&&endDate.setMonth(endDate.getMonth()+parseInt(months)),days&&endDate.setDate(endDate.getDate()+parseInt(days)),endDate.toLocaleString("ru-RU",{year:"numeric",month:"long",day:"numeric"})}export function dateToAddForm(startDate,days,months){let endDate=new Date(startDate);return months&&endDate.setMonth(endDate.getMonth()+parseInt(months)),days&&endDate.setDate(endDate.getDate()+parseInt(days)),endDate}